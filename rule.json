{
    "rules": {
        
        ".read": true,
        
        "questionVotesRecord" : {
            "$QuestionID" : {
                "votes" : {
                    "$VoteID" : {
                      ".write": "data.exists() && newData.exists() && newData.val() > data.val()"
                    }
                }
             }
        
        },
        
        "totalVotesCount" : {
             ".write": "data.exists() && newData.exists() && newData.val() > data.val()"
        }
 
        
    }
}